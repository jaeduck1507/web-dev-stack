<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="v">
	<!-- 회원별 장바구니 목록 조회 -->
	<select id="select_list" resultType="visit">
		select * from visit order by regdate
	</select>
	
	<insert id="visit_register" parameterType="visit">
		insert into visit values(
			seq_visit_idx.nextVal,
			#{name},
			#{content},
			#{pwd},
			#{ip},
			sysdate,
			#{filename}
		)
	</insert>
	
	<select id="visit_select_one" parameterType="int" resultType="visit">
		select * from visit where idx=#{idx}
	</select>
	
	<delete id="visit_delete" parameterType="int">
		delete from visit where idx=#{idx}
	</delete>
	
	<update id="visit_modify" parameterType="visit">
		update visit set content=#{content}, name=#{name}, ip=#{ip}, regdate=sysdate, filename=#{filename} where idx=#{idx}
	</update>
	
</mapper>












