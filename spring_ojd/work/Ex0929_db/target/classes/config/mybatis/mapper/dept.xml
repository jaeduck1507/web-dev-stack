<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="d">
	<select id="dept_list" resultType="vo.DeptVO">
		select * from dept	
	</select>
	
	<select id="dept_one" parameterType="int" resultType="vo.DeptVO">
		select * from dept where deptno = #{deptno}
	</select>

	<update id="modify_dept" parameterType="vo.DeptVO">
		update dept set dname = #{dname}, loc = #{loc} where deptno = #{deptno}
	</update>
	
	<delete id="delete_dept" parameterType="int">
		delete from dept where deptno = #{deptno}
	</delete>
	
	<insert id="insert_dept" parameterType="vo.DeptVO">
		insert into dept values(#{deptno}, #{dname}, #{loc})
	</insert>

</mapper>












